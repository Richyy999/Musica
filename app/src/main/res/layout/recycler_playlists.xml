<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="128dp"
    android:layout_height="wrap_content"
    android:layout_marginStart="24dp"
    android:layout_marginTop="8dp"
    android:layout_marginEnd="24dp"
    android:layout_marginBottom="16dp"
    android:background="?attr/colorFondoApp"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/contenedorPlaylist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <com.makeramen.roundedimageview.RoundedImageView
            android:id="@+id/imgPlaylist"
            android:layout_width="128dp"
            android:layout_height="128dp"
            android:contentDescription="@string/descImagenPlaylist"
            android:scaleType="centerCrop"
            android:src="@drawable/imagen_playlist"
            app:riv_corner_radius="20dp" />

        <TextView
            android:id="@+id/lblNombrePlaylist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="marquee"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:freezesText="true"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"
            android:singleLine="true"
            android:text="Playlist 1"
            android:textColor="?attr/colorTitulo"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/lblNumCancionesPlaylist"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="8dp"
            android:text="12 Canciones"
            android:textColor="?attr/colorSubtitulo"
            android:textSize="12sp" />

    </LinearLayout>

</FrameLayout>